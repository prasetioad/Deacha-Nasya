import React from 'react'

function Logo({color, size}) {
    return (
        <div style={{fontFamily: 'Aesthetic Violet', fontSize: size, color: color}}>
            <p>Deacha</p>
        </div>
    )
}

export default Logo
